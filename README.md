## 📣 실행방법

### Setup

```shell
$ touch dev.db
$ python3 -m venv venv
$ source venv/bin/activate
$ pip install -r requirements.txt
```

### run server

```shell
$ uvicorn main:app --reload
```
